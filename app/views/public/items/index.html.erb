<h3>商品一覧</h3>

<% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= attachment_image_tag item, :image, fallback: "no_image.png", size: "100x100" %>
  <% end %>
    <p>商品名　<%= item.name %></p>
    <p>¥ <%= converting_to_jpy(item.price) %></p>
<% end %>